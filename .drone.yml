kind: pipeline
type: docker
name: Docker

steps:
- name: Install
  image: node
  commands:
  - npm ci
- name: Build
  image: node
  commands:
  - npm run build
  - echo $demo
  environment:
    demo:
      from_secret: test
  when:
    branch:
    - main
